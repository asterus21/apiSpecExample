This is the example of the API specification given as a YAML file. The speicification is public as no server exists anymore.

***

Это пример API-спецификации, представленной в виде файла формата YAML. Спецификация публична, т.к. cервера больше не существует.
